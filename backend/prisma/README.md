# Prisma ORM Setup

This directory contains Prisma schema definitions, migrations, and database seed scripts.

## Commands

- **Generate Prisma Client**: `npm run prisma:generate`
- **Run Migrations**: `npm run prisma:migrate`
