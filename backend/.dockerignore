# Dependency & build outputs
node_modules
dist
coverage
.nyc_output

# Git & Editor config
.git
.gitignore
.vscode
.idea
*.swp
*.swo

# Environment files & secrets (should be injected at runtime via docker-compose/orchestrator)
.env
.env.*
!.env.example

# Debug logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Docker metadata (entrypoint.sh must remain copyable by Dockerfiles)
Dockerfile*
docker-compose*.yml
