/**
 * Resume worker - placeholder pending the Resume module's Prisma model.
 * Follow `workers/email.worker.ts`'s pattern once implemented.
 */
export const startResumeWorker = async (): Promise<void> => {};
