/**
 * Recommendation worker - placeholder pending the Recommendations module's
 * data sources. Follow `workers/email.worker.ts`'s pattern once implemented.
 */
export const startRecommendationWorker = async (): Promise<void> => {};
