/**
 * Job-sync worker - placeholder pending the Jobs module's Prisma model.
 * Follow `workers/email.worker.ts`'s pattern once implemented.
 */
export const startJobSyncWorker = async (): Promise<void> => {};
