/**
 * Interview worker - placeholder pending the Interviews module's Prisma
 * model. Follow `workers/email.worker.ts`'s pattern once implemented: a
 * `startXWorker()` function subscribing via `infrastructure/messaging`.
 */
export const startInterviewWorker = async (): Promise<void> => {};
