/**
 * Notification worker - placeholder pending the Notifications module's
 * Prisma model. Follow `workers/email.worker.ts`'s pattern once implemented.
 */
export const startNotificationWorker = async (): Promise<void> => {};
