/**
 * Job-sync queue producer - placeholder pending the Jobs module's Prisma
 * model. Intended to enqueue provider-sync jobs (src/providers/jobs/*)
 * once implemented, following `queues/email.queue.ts`'s pattern.
 */
export const JobSyncQueue = {};
