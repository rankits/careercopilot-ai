/**
 * Interview queue producer - placeholder pending the Interviews module's
 * Prisma model. Follow `queues/email.queue.ts`'s pattern once implemented:
 * a plain object of functions calling `messageBus.publishEvent(...)`.
 */
export const InterviewQueue = {};
