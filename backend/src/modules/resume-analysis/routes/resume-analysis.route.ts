import express from 'express';

const router = express.Router();

// TODO: add resume-analysis routes once the ResumeAnalysis Prisma model and
// resume-analysis.service.ts are implemented.

export default router;
