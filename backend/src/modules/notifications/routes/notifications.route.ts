import express from 'express';

const router = express.Router();

// TODO: add notifications routes once the Notification Prisma model and
// notifications.service.ts are implemented.

export default router;
