// Placeholder types for the Recommendations module.
// No Prisma model exists yet - this module is expected to read from the
// Jobs, Resume and User models once they exist and rank matches; add
// whatever persistence (if any) it needs to prisma/schema.prisma then.
export {};
