import express from "express";

const router = express.Router();

// TODO: add applications routes once the Application Prisma model and
// applications.service.ts are implemented.

export default router;
