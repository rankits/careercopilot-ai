import express from "express";

const router = express.Router();

// TODO: add resume routes (upload/parse/CRUD) once the Resume Prisma model
// and resume.service.ts are implemented.

export default router;
