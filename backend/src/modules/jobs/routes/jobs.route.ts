import express from "express";

const router = express.Router();

// TODO: add jobs routes (search/read, provider sync) once the Job Prisma
// model and jobs.service.ts are implemented.

export default router;
