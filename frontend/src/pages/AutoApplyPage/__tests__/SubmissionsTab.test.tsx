import { describe, expect, it } from 'vitest';

/** Skipped: see docs/assisted-apply-skipped-tests.md — replaced by AssistedApplicationsList (AA-080). */
describe.skip('SubmissionsTab AA-001', () => {
  it('deferred — see AssistedApplicationsList', () => {
    expect(true).toBe(true);
  });
});
